name: native
on:
  push: 
    branches: ["master"]
env:
  CARGO_TERM_COLOR: always
jobs:
  build:
    runs-on: rust
    steps:
    - uses: actions/checkout@v3
    - name: Build
      run: cargo build --release
